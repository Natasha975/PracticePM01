<Page x:Class="WpfApp.Accountant.WarehouseReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Accountant"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="10,5,10,10">
        <Label Content="Отчет по складам" HorizontalContentAlignment="Center" FontSize="17"/>
        <Grid>
            <DataGrid x:Name="WarehouseDataGrid" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название склада" Binding="{Binding НазваниеСклада}" />
                    <DataGridTextColumn Header="Тип склада" Binding="{Binding ТипСклада.Наименование}" />
                    <DataGridTextColumn Header="Общая сумма" Binding="{Binding ОбщаяСумма}" />
                    <DataGridTextColumn Header="Общее количество" Binding="{Binding ОбщееКоличество}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </StackPanel>
</Page>
